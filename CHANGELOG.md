# Changelog

所有值得关注的变更都记录在此文件中。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.1.0] — 2026-09-01

### 新增

- **单词打字模块**：逐字母着色、输错整词重置（Qwerty Learner 核心机制）、WPM 与正确率统计、系统 TTS 朗读、多词库切换、每组 20 词、完成总结面板。
- **连词造句模块**：句子拆词块打乱、流式点选排序、前缀即时纠错、点击已选词块回退、完成后朗读原句（Earthworm 核心机制）。
- **口语私教模块**：TTS 示范朗读、麦克风录音、系统语音识别（优先 on-device 离线）、自研 LCS 词级对齐发音评分（准确度 / WPM / 漏词 / 评级），识别不可用时优雅降级为跟读模式。
- **复习模块**：跨模块统一到期队列、翻转卡片、SM-2 四档评分（忘记/困难/良好/简单）、没记住的卡片本轮末尾重现。
- **学习中心首页**：待复习 / 今日练习 / 连续天数 / 已学 / 已掌握 五维概览、四个模块入口、今日新学目标进度、最近 7 天活跃柱状图。
- **学习统计页**：累计打字 / 造句 / 口语 / 复习量、连续天数、打字正确率、七日活跃图。
- **统一记忆曲线**：打字 / 造句 / 口语练习结果均写入共享 `MemoryItem`，到期后统一进入复习队列。
- **菜单栏常驻**：LSUIElement 无 Dock 图标，NSStatusItem 含「显示/隐藏」「Nexus 应用」子菜单（Hub / KeyHub）「退出」。
- **全局热键**：`⌃⌥E` 唤出/隐藏窗口。
- **URL Scheme**：`nexus-engram://` 唤出，`nexus-engram://page?name=<模块>` 深链直达。
- **副屏优先定位**：启动时自动将窗口放到外接显示器（无副屏则居中主屏）。
- **离线语音**：AVSpeechSynthesizer TTS + SFSpeechRecognizer（requiresOnDeviceRecognition 优先），零云 API Key。
- **内容驱动**：5 个内置 JSON 内容包（四级词 / 程序员词 / 日常句 / 商务句 / 口语场景），新增内容零代码改动。
- **持久化**：进度与每日统计保存到 `~/Library/Application Support/Engram/progress.json`，连续天数自动维护。

### 技术

- 纯 Swift + Cocoa/AppKit，swiftc 直接编译，零第三方依赖。
- 五层架构（App / Models / Services / Views / Common）单向依赖，所有 Service 先定义 Protocol。
- 42 项逻辑层断言测试（SM-2 / 打字状态机 / 造句校验 / 发音评分 LCS / 内容加载 / 持久化）。
- 内容 JSON 机器校验脚本（id 全局唯一、必填字段、合法枚举）。
- 全量编译零警告。

### 已知限制

- 口语识别依赖系统英文语音识别可用性；部分设备可能不支持 on-device 识别，此时自动回退在线识别或跟读模式。
- 打字页键码映射基于 ANSI 物理布局，非 ANSI 键盘的特殊字符可能不响应（字母键均正常）。
- 窗口固定宽度 940pt，仅纵向可缩放（Nexus 家族惯例，避免布局重排）。
